[project]
name = "decicontas-workspace"
version = "0"
description = "Workspace root for the DeciContas.br monorepo."
requires-python = ">=3.12,<3.13"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["tools", "backend"]
